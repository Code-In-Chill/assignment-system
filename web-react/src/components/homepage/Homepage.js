import React from 'react';
import Login from "../login/Login";

const Homepage = () => {
    return (
        <div className={"Homepage"}>
            Homepage
            <Login/>
        </div>
    );
};

export default Homepage;